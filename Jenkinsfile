node {
        stage('Checkout') {
            checkout scm
            echo "cloned like a goat"
        }

        stage('Build') {
            echo "let's get started"
            sleep 5
            echo "almost"
            sleep 4
            echo "finished"
        }
}
